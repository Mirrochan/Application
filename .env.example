# Copy this file to .env and replace values before running docker-compose

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=Event_Managment_Db

# Backend / .NET
ASPNETCORE_ENVIRONMENT=Development
CONNECTION_STRING=Host=db;Port=5432;Database=Event_Managment_Db;Username=postgres;Password=changeme
JWT_SECRET=replace_with_a_strong_secret

# Frontend
# If frontend needs runtime config, add e.g. API_URL=http://localhost:5107
API_URL=http://localhost:5107
